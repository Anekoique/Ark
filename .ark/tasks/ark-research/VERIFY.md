# `ark-research` VERIFY

> Status: Living document. Maintained by the implementer during EXECUTE → COMMIT.
> Feature: `ark-research`
> Target Task: `ark-research`
> Tier: `deep`
>
> Each checklist item resolves to PASS | FAIL (with explanation) | N/A (with explanation). Findings (`V-NNN`) capture cross-cutting observations with a Resolution. **No verdict line — completion = no `PENDING`.** Deep tier: `/ark:commit` refuses on any `PENDING`. Standard: warns and proceeds.

---

## Project Spec Compliance

> Auto-seeded from `.ark/specs/project/INDEX.md` at `task verify` time, walked recursively. Renders two subsections: `Index integrity` (one PENDING per discovered `INDEX.md` — does it enumerate all on-disk children?) and `Leaf SPECs` (one rolled-up PENDING for `LAYOUT.md` conformance plus a traceability sublist of every leaf).

### Index integrity

- [x] `INDEX.md` enumerates all children of `specs/project/`: PASS — `.ark/specs/project/INDEX.md` rows `LAYOUT.md`, `rust/COMMENTS.md`, `rust/STYLE.md`, `rust/ERRORS.md`; on-disk children are exactly those four files; no missing or orphan entries.

### Leaf SPECs

- [x] All leaf SPECs under `specs/project/` conform to `LAYOUT.md`: PASS — V-001 resolved in this iteration; the eight offending docstrings were rewritten to describe behavior without naming the validation row, so `rust/COMMENTS.md` C-23 ("Never use task-mark tags in source") now holds. Leaf SPECs themselves are unchanged and structurally conform (each carries `[**Purpose**]`, `[**Rules**]`, `[**Exceptions**]` plus optional sections per L-1; one rule prefix per file per L-2; `LAYOUT.md` cited per L-4).
  - `LAYOUT.md` — PASS (not touched; structure matches its own L-1..L-8).
  - `rust/COMMENTS.md` — PASS — V-001's eight doc-comment sites rewritten; no `V-UT-*` / `V-F-*` / `C-*` SPEC-rule labels remain in `crates/`. Existing third-person/punctuation/non-contraction discipline (C-3, C-14, C-17) honored throughout.
  - `rust/STYLE.md` — PASS — `cargo fmt --all -- --check` exits 0; `cargo clippy --workspace --all-targets -- -D warnings` exits 0; new variants follow `UpperCamelCase` (`Tier::Research`, `Phase::Research`); new test function names follow `snake_case`; trailing commas, block-indent, 100-col cap all observed by rustfmt; no `Tier::*`-style task-marker labels in source code identifiers.
  - `rust/ERRORS.md` — PASS — no new `Error` variant added (reuses `Error::WrongTier`, `Error::IllegalPhaseTransition` per C-16); no `unwrap()` outside test code in the new arms; no bare `#[from]` introduced; the new `research_corpus_files` helper returns `Vec::new()` on `walk_files` error via `unwrap_or_default()` which is acceptable for a best-effort directory walk (absent or unreadable `research/` is not a fatal condition per C-7).

## Related Feature Spec Compliance

> Auto-seeded from PRD's `[**Related Specs**]`. Empty when none.

- [x] specs/features/ark-agent-namespace/SPEC.md: PASS — Tier and Phase enums grew non-breakingly (existing serde tags untouched, new tags `"research"` added with `#[serde(rename_all = "lowercase")]`); `can_transition` adds exactly the two declared tuples and rejects every other `(Research, _, _)` (verified by `can_transition_research` cross-product test in `state.rs:389`); `check_phase_for_commit`'s matrix grew the `(Tier::Research, Phase::Research)` arm at `commit.rs:307` and rejects every other Research-phase as `IllegalPhaseTransition { to: Committed }`.
- [x] specs/features/subagent-support/SPEC.md: PASS — `ark-researcher` contract reused unchanged. The slash-command bodies dispatch to the existing `ark-researcher` subagent and reference its Write-ALLOWED path `.ark/tasks/<slug>/research/*.md` (G-4). No agent prompt edits; no new agents shipped. The Codex SKILL.md correctly does NOT rewrite `ark-researcher` (the subagent name was never a `/ark:` slash-command form); the substitution map applies only to `/ark:research`, `/ark:quick`, `/ark:design`, `/ark:commit`.
- [x] specs/features/task-concurrency-control/SPEC.md: PASS — focus model reconciles correctly: `task_new` for research tier registers `state.tasks.active` and binds `state.focus` via the same `register_focus` path used by the other tiers (`new.rs:281` inside `scaffold_inside_worktree`; `new.rs:156` in the no-worktree path); `task_commit` clears focus via the same `clear_focus_if_matches` call on research tier (`commit.rs:280`). The `task_toml_loads_without_optional_fields` test in `state.rs:560` covers the V-E-4 round-trip invariant for pre-existing tasks (the test predates this iteration but its assertion now also implicitly covers research-tier introductions via the `#[serde(rename_all)]` boundary).
- [x] specs/features/detachable-feature-spec/SPEC.md: PASS — research tier branch in `task_commit` does NOT call `parse_spec_path`, `spec_extract`, or `spec_register`. Verified at `commit.rs:178-207`: `let deep = prev_toml.tier == Tier::Deep` then both the `parse_spec_path` call AND the `if deep { ... spec_extract ... spec_register ... }` block are gated on `deep`. Research tier seeds `feature_segments = vec![opts.slug.clone()]` purely for the legacy default; `ark_files_for_first_commit` for research tier explicitly excludes `spec_path` and `intermediate_indexes` (verified at `commit.rs:399-407`). The e2e test `research_tier_commit_does_not_promote_spec` (`commit.rs:1677`) asserts both that no `features/<slug>/SPEC.md` lands AND that the features INDEX gains no row for the research slug.
- [x] specs/features/worktree/SPEC.md: PASS — `--worktree` is opt-in across all tiers including research. `task_new_research_with_worktree_succeeds` (`new_tests.rs:571`) covers V-UT-10: the worktree-first scaffold produces the same `branch = "feat/<slug>"` / `worktree_path = ".ark/worktrees/feat/<slug>"` / `base_branch = "main"` topology as deep tier. `build_task_toml` does not vary the worktree-related fields by tier; only `phase` and `max_iterations` switch on tier (`new.rs:357-365`). No `worktree` SPEC body change required.
- [x] specs/features/ark-context/SPEC.md: PASS — no `ContextOptions`, `Scope`, `PhaseFilter`, or `ProjectedContext` schema change. The `design`-phase projection still serves freshly-seeded PRD tasks; research-tier tasks reuse it verbatim (`workflow.md` line 22 in the Research subsection explicitly references `ark context --scope phase --for design`). Snapshot `SCHEMA_VERSION` is not touched.
- [x] specs/features/workspace/SPEC.md: PASS — `record_workspace_journal` in `commit.rs:498-549` is invoked unconditionally on every tier when `!opts.no_commit`. Research-tier task closure stamps the journal identically to other tiers; `task.toml.journal_path` is set when identity exists; `clear_focus_if_matches` runs on the research path. No tier-specific gating of the workspace write path.

## PRD Constraints

> Auto-seeded from PRD's `[**Outcome**]` (and `[**Constraints**]` when present). One bullet per criterion.

- [x] `/ark:research "<topic>"` creates a research task at `.ark/tasks/<slug>/` with `tier = "research"`, `phase = "research"`, a seeded PRD scoped for research semantics, and binds focus to the slug: PASS — `parse_tier("research") → Ok(Tier::Research)` at `agent_cli.rs:329`; `build_task_toml` sets `phase = Phase::Research` for research tier (`new.rs:357-360`); `state_mutate` registers the slug as active and binds focus via `register_focus` (`new.rs:156` / `new.rs:281`). `task_new_with_tier_research_starts_in_phase_research` (`new_tests.rs:548`) asserts the resulting `task.toml` carries `tier = research, phase = research, max_iterations = None`.
- [x] Iterative `ark-researcher` dispatches populate `.ark/tasks/<slug>/research/<topic>.md` files exactly as they do today on deep tier: PASS — `ark-researcher` Write-ALLOWED path is `.ark/tasks/<slug>/research/*.md` (subagent-support SPEC C-8). The slash command bodies on all three platforms direct main session to dispatch the existing `ark-researcher` subagent; no new agent prompt, no path contract change.
- [x] `/ark:commit -m "<msg>"` closes a research task atomically: stages `task.toml` + `PRD.md` + `research/**`, commits, marks `phase = Committed`. No VERIFY gate, no SPEC extraction, no `[**SPEC Path**]` requirement: PASS — `commit.rs:146-166` gates the VERIFY block on `matches!(prev_toml.tier, Tier::Standard | Tier::Deep)`; research tier falls through with `pending = VerifyPendingCounts::default()`. The deep-spec branch at `commit.rs:174-207` is gated on `deep = prev_toml.tier == Tier::Deep`. `ark_files_for_first_commit` on `Tier::Research` walks `<task_dir>/research/` recursively (`commit.rs:405-407`) and does NOT include the SPEC or features INDEX (`commit.rs:399`). E2E tests `research_tier_commit_stages_corpus_and_clears_focus`, `research_tier_skips_verify_gate`, `research_tier_commit_succeeds_with_empty_corpus`, `research_tier_commit_does_not_promote_spec` (`commit.rs:1593-1703`) cover the full contract.
- [x] `ark agent task plan` / `review` / `execute` / `verify` on a research task error out with `IllegalPhaseTransition` — the only legal transition from `Research` is `Committed`: PASS — `can_transition` allows only `(Research, Research, Committed)` and `(Research, Committed, Archived)` (`state.rs:191-192`); every other Research-source tuple returns `false` via the catch-all `_ => false` arm. `research_tier_illegal_transition_writes_no_artifact` (`phase.rs:330`) confirms `task_plan`, `task_review`, `task_execute`, `task_verify` all yield `IllegalPhaseTransition` AND leave `task.toml` byte-identical AND seed no artifact on disk.
- [x] `--worktree` is accepted on research tier (user choice), not required: PASS — `TaskNewOptions.worktree: Option<TaskNewWorktree>` is `None` by default; nothing in the research branch sets it unconditionally; `task_new_research_with_worktree_succeeds` (`new_tests.rs:571`) confirms the optional flag flows through to the worktree-first scaffold path.
- [x] `ark archive` moves committed research tasks into the YYYY-MM bucket like any other tier: PASS — `derive_archive_month` in `agent_cli.rs:617` reads `task.toml.committed_at` regardless of tier; the archive transition `(Committed, Archived)` is legal for Research per `state.rs:192`. No tier-specific gating of `task_archive_move`. Asserted indirectly by `archived_only_reachable_from_committed` and `archived_is_terminal` (`state.rs:466`, `state.rs:489`) which now iterate over `Tier::Research`.
- [x] All three platforms ship `/ark:research`: Claude and OpenCode bodies byte-identical modulo frontmatter; Codex applies the documented substitution map: PASS — direct diff of `templates/{claude,opencode}/commands/ark/research.md` modulo frontmatter is empty. The parity test `research_slash_command_claude_and_opencode_bodies_match` (`templates.rs:717`) asserts byte-identity. The Codex inverse-substitution test `research_codex_skill_matches_claude_under_inverse_substitution` (`templates.rs:748`) applies `ark-research <topic> → /ark:research $ARGUMENTS`, `ark-research → /ark:research`, `ark-quick → /ark:quick`, `ark-design → /ark:design`, `ark-commit → /ark:commit` and asserts byte-equality. The Codex body preserves `ark-researcher` (the subagent) unchanged — `ark-researcher` was never a slash-command form, consistent with C-11's "every `/ark:<name>` substring" wording.
- [x] workflow.md explains: when to use research vs. embedded researcher dispatch; PRD-on-research semantic remap (Outcome optional; SPEC Path ignored; Related Specs optional): PASS — `templates/ark/workflow.md` ##Tiers bullet (line 44), lifecycle arrow second line (line 79), and the new ## Research subsection (lines 260-281) cover all three required items: when-to-use distinction in the bullet pair at lines 266-267; PRD field remap at lines 271-275; cross-over by fresh `task new` at line 279. Subsection is 21 lines, within the C-15 ≤30 line budget.
- [x] Existing tasks of tier `quick` / `standard` / `deep` continue to round-trip through `task.toml` load/save with no migration: PASS — `Tier` and `Phase` enums grew with new variants under `#[serde(rename_all = "lowercase")]`; existing serde tags (`"quick"`, `"standard"`, `"deep"`, `"design"`, `"plan"`, …) are unchanged. The `task_toml_loads_without_optional_fields` test (`state.rs:559`) confirms a pre-iteration `task.toml` still loads. No `task.toml` schema field additions in this iteration.

## Plan Fidelity

> Auto-seeded from the latest `NN_PLAN.md`'s `## Spec` Goals (`G-N`). PASS when delivered, FAIL when not, N/A when withdrawn (PLAN's Log explains).

- [x] G-1: `Tier::Research` is a first-class variant alongside Quick/Standard/Deep: PASS — `Tier::Research` variant present at `state.rs:26` with serde tag `"research"`; `parse_tier` accepts `"research"` at `agent_cli.rs:329`; round-trip test `tier_research_round_trips_as_lowercase` at `state.rs:432` asserts the TOML literal `tier = "research"`.
- [x] G-2: `Research → Committed → Archived` is the entire legal lifecycle for tier=research: PASS — `can_transition` grants exactly `(Research, Research, Committed)` and `(Research, Committed, Archived)` at `state.rs:191-192`. `can_transition_research` (`state.rs:389`) walks the full Phase × Phase cross-product and asserts only those two arms return `true`. `archived_only_reachable_from_committed` and `archived_is_terminal` (`state.rs:466`, `state.rs:489`) now iterate over `Tier::Research` as well, locking in the terminal-phase property.
- [x] G-3: `task new --tier research` scaffolds PRD.md + task.toml; --worktree is opt-in: PASS — `build_task_toml` selects `Phase::Research` for `Tier::Research` (`new.rs:357-360`); the rest of `task_new_no_worktree` is tier-independent (copies PRD template, writes task.toml, registers focus). `task_new_with_tier_research_starts_in_phase_research` (`new_tests.rs:548`) covers the no-worktree path; `task_new_research_with_worktree_succeeds` (`new_tests.rs:571`) covers the opt-in worktree path with the same topology as deep tier.
- [x] G-4: `task commit` on research tier skips VERIFY and SPEC extract; stages task.toml + research/**: PASS — VERIFY gate is conditional on `matches!(prev_toml.tier, Tier::Standard | Tier::Deep)` (`commit.rs:146`); SPEC branch is conditional on `deep` (`commit.rs:189`); `ark_files_for_first_commit` on `Tier::Research` appends `research_corpus_files(task_dir)` to the file list (`commit.rs:405-407`) and excludes SPEC/INDEX (`commit.rs:399`). E2E `research_tier_commit_stages_corpus_and_clears_focus` (`commit.rs:1593`) asserts the closing commit includes `.ark/tasks/<slug>/task.toml` + every file under `research/` (including nested subdirs) and does NOT include any `features/<slug>/SPEC.md`.
- [x] G-5: `/ark:research` ships on Claude / Codex / OpenCode; Claude+OpenCode byte-identical bodies, Codex applies the documented substitution map: PASS — three template files present: `templates/claude/commands/ark/research.md`, `templates/codex/skills/ark-research/SKILL.md`, `templates/opencode/commands/ark/research.md`. Byte-identity and inverse-substitution parity tests (`templates.rs:717`, `templates.rs:748`) both pass. Frontmatter is per-platform (`description: ...` for Claude, `description: ... argument-hint: ...` for OpenCode is identical Claude minus the argument-hint, `name: ark-research description: ...` for Codex).

## Plan Fidelity — Constraints (C-1 through C-18)

> Each Constraint from `02_PLAN.md`'s `## Spec` is enumerated below.

- [x] C-1: `Tier::Research` serializes/deserializes as `"research"` via `#[serde(rename_all = "lowercase")]`; pre-existing tier tags unchanged: PASS — `state.rs:14-15` carries the existing `#[serde(rename_all = "lowercase")]` on `enum Tier`; the new `Research` variant inherits it. `tier_research_round_trips_as_lowercase` at `state.rs:432`.
- [x] C-2: `Phase::Research` serializes/deserializes as `"research"`: PASS — `state.rs:30-32` same rename_all attribute on `enum Phase`. `phase_research_round_trips_as_lowercase` at `state.rs:449`.
- [x] C-3: `can_transition` returns `true` for exactly two new tuples; every other `(Research, _, _)` returns `false`: PASS — table at `state.rs:191-192`; cross-product test `can_transition_research` (`state.rs:389`) walks 8×8 phase combinations.
- [x] C-4: `task new --tier research` writes `phase = Research`: PASS — `build_task_toml` at `new.rs:357-360`; covered by `task_new_with_tier_research_starts_in_phase_research`.
- [x] C-5: `task new --tier research --worktree` succeeds with the same worktree topology as deep tier: PASS — `task_new_research_with_worktree_succeeds` (`new_tests.rs:571`) asserts branch=`feat/lit-review`, base=`main`, project-relative path matches deep tier's pattern. The flag is opt-in per `worktree` SPEC G-4 (no tier-coupled requirement).
- [x] C-6: `task_commit` on `Tier::Research` skips `parse_verify_md` and `parse_spec_path`/`spec_extract`/`spec_register`; `deep_spec_promoted = false`: PASS — VERIFY gate skipped at `commit.rs:146`; the `deep` flag drives the SPEC branch at `commit.rs:189`; `deep_spec_promoted: deep` in the summary at `commit.rs:236` and `commit.rs:286` — research tier returns `false`. `research_tier_skips_verify_gate` (`commit.rs:1638`) asserts commit succeeds with no VERIFY.md on disk; `research_tier_commit_does_not_promote_spec` (`commit.rs:1677`) asserts the SPEC file does not exist post-commit.
- [x] C-7: `task_commit` on `Tier::Research` stages `task.toml` plus every file under `research/` recursively; absent `research/` is not an error: PASS — `research_corpus_files` at `commit.rs:418` uses `walk_files(...).unwrap_or_default()`, returning empty Vec when the directory is absent; `ark_files_for_first_commit` includes the walk output unconditionally on `Tier::Research` (`commit.rs:405`). `research_corpus_files_walks_recursively` (`commit.rs:961`) covers nested subdirs; `research_tier_commit_succeeds_with_empty_corpus` (`commit.rs:1658`) covers the absent-directory case.
- [x] C-8: `check_phase_for_commit` accepts `(Tier::Research, Phase::Research)`; every other phase under `Tier::Research` returns `IllegalPhaseTransition`: PASS — matrix at `commit.rs:307-313` includes the new arm; `check_phase_for_commit_accepts_only_legal_inputs` (`commit.rs:830`) walks the rejection set including `(Research, Design)`, `(Research, Plan)`, `(Research, Verify)`, `(Research, Execute)`.
- [x] C-9: `ark agent task plan|review|execute|verify` on a research task fails with `IllegalPhaseTransition`; no `task.toml` mutation; no artifact written: PASS — `research_tier_illegal_transition_writes_no_artifact` (`phase.rs:330`) drives all four verbs, asserts `IllegalPhaseTransition`, asserts `task.toml` byte-identical pre/post, asserts neither `PLAN.md`, `00_PLAN.md`, `00_REVIEW.md`, nor `VERIFY.md` was written. The artifact suppression is guaranteed by the early-return at `phase.rs:123-125`.
- [x] C-10: PRD template is reused unchanged; the slash command body documents the semantic remap: PASS — `task_new` for research tier calls `copy_template("PRD", ...)` via the shared `task_new_no_worktree` / `scaffold_inside_worktree` paths; no PRD template variant introduced. Slash command body at lines 41-47 of each platform's research.md carries the field remap (What/Why/Outcome/Related Specs/SPEC Path).
- [x] C-11: per-platform variant contract (Claude/OpenCode byte-identical; Codex closed-form substitution map): PASS — `research_slash_command_claude_and_opencode_bodies_match` (`templates.rs:717`) asserts byte-identity after frontmatter strip; `research_codex_skill_matches_claude_under_inverse_substitution` (`templates.rs:748`) applies the inverse map and asserts byte-equality. The substitution covers `/ark:research → ark-research`, `/ark:quick → ark-quick`, `/ark:design → ark-design`, `/ark:commit → ark-commit`, `$ARGUMENTS → <topic>`, and the H1 reshape. `ark-researcher` (the subagent) is left alone on both sides — it was never a `/ark:` slash-command form. The closed-form rule covers every `/ark:<name>` substring in the body without enumeration drift.
- [x] C-12: Slash command names `ark-researcher` as dispatch target and mirrors `/ark:quick`'s `[USER]` staging step verbatim: PASS — Step 4 of each body dispatches `ark-researcher`; Step 5 says "User runs `git add .ark/tasks/<slug>/`. Then invoke `/ark:commit -m \"<message>\"`. See `/ark:commit` for the contract." This is the verbatim staging step from `quick.md` modulo whitespace.
- [x] C-13: `workflow.md` `## Tiers` bullet list grows a Research bullet at the end: PASS — `templates/ark/workflow.md:44` has `- **Research** — knowledge-gathering; corpus IS the deliverable; follow-up implementation optional. Artifacts: PRD.md, research/.` as the fourth bullet after Quick / Standard / Deep.
- [x] C-14: lifecycle arrow gains a second `RESEARCH → COMMIT → ARCHIVE` line: PASS — `templates/ark/workflow.md:79`: `RESEARCH → COMMIT → (later) ARCHIVE          research tier (no PLAN/REVIEW/EXECUTE/VERIFY)`. The existing primary arrow at line 77 is unchanged.
- [x] C-15: `workflow.md` ships a Research subsection ≤30 lines covering when-to-use, PRD field remap, fresh `task new` cross-over: PASS — lines 260-281, 21 lines total, all three items present (when-to-use distinction at 266-267, PRD field remap at 271-275, fresh `task new` at 279).
- [x] C-16: No new `Error` variants; no `task.toml` schema additions; no `Layout` getter additions: PASS — `grep -n "Research" crates/ark-core/src/error.rs` and `crates/ark-core/src/layout.rs` returns zero hits. `state.rs:62-128` `struct TaskToml` schema is unchanged in this iteration. The new `task_promote` rejection reuses the existing `Error::WrongTier { expected: Tier, actual: Tier }`.
- [x] C-17: `Tier::Research` is never reached at any production site that branches on `Tier::Deep`: PASS — four independent gates verified: (a) `check_phase_for_commit` at `commit.rs:307` rejects every `(Research, X≠Research)` tuple before the VERIFY block runs; (b) `check_transition` at `state.rs:198` (called from `phase.rs:83`) rejects every illegal Research transition and the head-of-function early-return at `phase.rs:123-125` ensures no artifact is seeded even if `transition()` is reordered; (c) the source-or-target Research early-return at `promote.rs:69-74` fires before `phase_exists_in_tier`; (d) `build_task_toml` at `new.rs:357-360` selects `Phase::Research` directly so no Deep-only initial-phase code runs. C-17 is an invariant on Research-tier reachability per R-004's restatement.
- [x] C-18: `task_promote` rejects every invocation involving `Tier::Research` with `Error::WrongTier`: PASS — `promote.rs:69-74` early-returns on `from == Tier::Research || opts.to == Tier::Research`. `task_promote_rejects_research_source` (`promote.rs:287`) and `task_promote_rejects_research_target` (`promote.rs:315`) cover V-UT-13 (a) and (b) including the byte-identical `task.toml` pre/post assertion.

## SPEC Drift

- [x] Modified feature SPECs have CHANGELOG entries: N/A in the touched-SPEC sense (no pre-existing feature SPEC was modified by this task — the seven Related Specs all carry no body changes). PASS for the about-to-promote sense: V-002 resolved by patching `02_PLAN.md`'s `## Spec` API-Surface block to replace the inlined 80-line markdown body example with a one-paragraph reference to the canonical files under `templates/{claude,opencode,codex}/.../research`. Verified via the `awk '/^## Spec$/{flag=1;next} /^## /{if(flag){print "STOP at line " NR ": " $0; exit}} flag'` extractor simulator: it now terminates at `## Runtime` (line 271), which is the intended boundary, so the full Goals/Non-goals/Architecture/Data Structure/API Surface/Constraints sweep promotes correctly into the future `.ark/specs/features/ark-research/SPEC.md`.

## Findings

> Cross-cutting observations that don't map to a single seeded item. Each Finding has a Resolution; `/ark:commit` requires every Resolution to be non-PENDING.

## Severity Summary: 0 CRITICAL · 0 HIGH · 0 MEDIUM · 0 LOW (all three findings resolved in-iteration)
## Verification: build PASS · tests PASS (564 across all binaries/libs) · lint PASS · format PASS

### V-001 SPEC-rule labels in new doc-comments violate `rust/COMMENTS.md` C-23

- **Severity:** HIGH
- **Location:**
  - `crates/ark-core/src/commands/agent/task/promote.rs:282` — `/// V-UT-13(a): promoting *out of* \`Tier::Research\` is rejected.`
  - `crates/ark-core/src/commands/agent/task/promote.rs:313` — `/// V-UT-13(b): promoting *into* \`Tier::Research\` is rejected.`
  - `crates/ark-core/src/commands/agent/task/new_tests.rs:545` — `/// V-UT-9: \`task_new\` with \`Tier::Research\` writes \`phase = Research\` in the …`
  - `crates/ark-core/src/commands/agent/task/new_tests.rs:568` — `/// V-UT-10: \`task_new\` with \`Tier::Research --worktree\` produces the same …`
  - `crates/ark-cli/src/agent_cli.rs:340` — `/// V-UT-15: \`parse_tier\` accepts the four canonical tier names …`
  - `crates/ark-core/src/commands/agent/task/commit.rs:826` — `/// V-UT-8: Verifies the precondition table — only \`(Quick, Execute)\`, …`
  - `crates/ark-core/src/commands/agent/task/commit.rs:956` — `/// V-UT-11 / V-UT-12: \`research_corpus_files\` returns every file under …`
  - `crates/ark-core/src/commands/agent/task/commit.rs:1656` — `/// when only PRD is staged (V-F-3 — corpus is optional).`
- **Problem:** `rust/COMMENTS.md` C-23 states unambiguously: "Never use task-mark tags in source. Identifiers like `V-UT-1`, `C-11`, `G-7`, or other SPEC-rule labels never appear inside `crates/` comments. The constraint they encode goes inline as prose; the label goes nowhere." The newly added doc-comments above each carry V-UT-* / V-F-* labels at the head of the docstring, mapping each test to its Plan validation ID. The constraint they encode is fine; the label is forbidden.
- **Why it matters:** Process metadata (PLAN validation IDs, finding IDs, task slugs) leaks from the workflow artifacts into shipping source. Future readers see opaque identifiers without context. The rule exists because these labels rot the moment the PLAN they reference is archived. C-8 reinforces the same point ("Don't reference task names, archived task slugs, PR numbers, or other out-of-tree process artifacts in source comments").
- **Recommendation:** Rewrite each docstring so it states the behavior being verified without naming the validation row. Example: `/// V-UT-13(a): promoting *out of* \`Tier::Research\` is rejected.` becomes `/// Promoting a research-tier task to another tier is rejected.` The function-name convention already encodes intent; the label is redundant. Apply to all eight sites; do NOT touch pre-existing V-IT-1/V-IT-2/V-IT-3/V-E-1 labels at `new_tests.rs:450/452/478/505/523` — those predate this task and are outside its blast radius.
- **Resolution:** FIXED — all eight docstrings rewritten in this iteration (commit.rs:826, 956, 1654; new_tests.rs:524, 547; promote.rs:282, 312; agent_cli.rs:339). Pre-existing labels at the cited legacy sites are left intact per the recommendation's blast-radius bound. `cargo build` / `test` / `fmt` / `clippy` all remain green post-edit.

### V-002 Deep-tier SPEC extraction will truncate when `02_PLAN.md`'s `## Spec` embeds H2 lines inside a fenced code block

- **Severity:** CRITICAL
- **Location:**
  - `.ark/tasks/ark-research/02_PLAN.md:244-323` — the `## Spec` section embeds a ```markdown … ``` fence containing `## Preconditions` (line 260), `## Steps` (268), `## If the corpus turns into implementation` (309), and `## See Also` (319).
  - `crates/ark-core/src/commands/agent/spec/extract.rs:175-198` — `extract_spec_section` is line-based; `is_section_boundary` matches `line.starts_with("## ")` regardless of fenced-code-block state.
- **Problem:** When `/ark:commit` runs against this deep-tier task, `spec_extract` will scan from `## Spec` (line 55) and stop at the first H2-matching line — `## Preconditions` at line 260 — which lives inside a markdown code fence. The extracted SPEC body will end mid-`[**API Surface**]`, losing C-11..C-18, the Trade-offs section, the Validation section, and the Acceptance Mapping. The promoted `.ark/specs/features/ark-research/SPEC.md` will be substantially incomplete; CHANGELOG entries against later iterations would mask the loss.
- **Why it matters:** This is the canonical SPEC promotion path; truncation defeats the purpose of capturing the durable design record. The task statement explicitly assumed "the extraction boundary is well-defined" given the trailing `## Runtime`; that assumption holds for a parser that respects code fences but does NOT hold for the current line-based implementation. The PLAN cannot ship this SPEC cleanly without either (a) escaping the embedded H2s in the API-Surface code block, or (b) hardening `extract_spec_section` to track fenced-code-block state.
- **Recommendation:** Two viable resolutions:
  1. **Patch the PLAN** (minimum scope): in `02_PLAN.md`'s API-Surface code block, replace the bare `##` H2 lines inside the embedded markdown example with `\#\#` escaped form, OR replace the example with an inline excerpt rather than a full reproduction of the slash-command body. Cite the canonical source in `templates/claude/commands/ark/research.md` instead of reproducing it.
  2. **Harden the extractor** (durable fix): teach `extract_spec_section` to skip lines while inside a `` ``` `` fence. Add a unit test in `extract.rs` whose body includes a fenced H2 inside the `## Spec` section and asserts the extraction does NOT terminate at the embedded line. This is the right long-term fix but expands this task's scope.
  The main session should pick (1) as the immediate unblock; (2) belongs in a follow-up `quick` or `standard` task. Either way, the task cannot commit cleanly until one of them is applied.
- **Resolution:** FIXED — option (1) applied. The 80-line embedded markdown body example at lines 244-323 of `02_PLAN.md` was replaced with a single paragraph naming `templates/{claude,opencode}/commands/ark/research.md` and `templates/codex/skills/ark-research/SKILL.md` as the canonical sources, plus a prose summary of the body's five-step structure that contains no bare H2 lines. The `awk` extractor simulator confirms the next `## ` after `## Spec` is now `## Runtime` at line 271 (the intended boundary), so the full Spec section will promote cleanly. Option (2) — hardening `extract_spec_section` to track fenced-code-block state — is left as a follow-up improvement; the new prose-by-reference shape is durable on its own.

### V-003 CLI doc-comment for `--tier` lists only three tiers, not four

- **Severity:** LOW
- **Location:** `crates/ark-cli/src/agent_cli.rs:185` — `/// Task tier (\`quick\`, \`standard\`, or \`deep\`).`
- **Problem:** The CLI help text for `--tier` enumerates only `quick`, `standard`, `deep` while `parse_tier` (line 324) now accepts `research` as well, and the error message at line 330 correctly lists all four. The doc-comment is therefore stale and inconsistent with the implementation. Users running `ark agent task new --help` will not see `research` as an accepted value through the help renderer (clap derives the long description from this doc-comment).
- **Why it matters:** Discoverability — the slash command body documents the verb, but a user who jumps straight to `ark agent task new --help` won't learn the research tier exists from clap's output. C-9 in COMMENTS.md ("Don't restate the type system") doesn't apply here because the enum lacks `value_enum`-derived help and the doc-comment IS the discovery surface.
- **Recommendation:** Rewrite line 185 to `/// Task tier (\`quick\`, \`standard\`, \`deep\`, or \`research\`).` matching the error message at line 330. One-line edit, no API change.
- **Resolution:** FIXED in `crates/ark-cli/src/agent_cli.rs:185` — doc-comment now enumerates all four canonical tier names, matching `parse_tier`'s error message.

## Notes

- Build / test / lint / format: all four commands run clean. `cargo build --workspace` exits 0; `cargo test --workspace` exits 0; `cargo fmt --all -- --check` exits 0; `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
- Iteration 02 PLAN's `## Log` correctly absorbs all four R-NNN findings and both trade-off items from `01_REVIEW.md`; the Response Matrix matches what shipped in code. No residual scope drift.
- `task_promote_rejects_research_source` and `task_promote_rejects_research_target` (V-UT-13 (a) / (b)) are the load-bearing assertions for the no-research-cross-over invariant; both cover the byte-identical `task.toml` pre/post check that distinguishes a "rejected before mutation" path from a "mutated then rolled back" path.
- The Codex inverse-substitution parity test deliberately exercises `ark-research <topic> → /ark:research $ARGUMENTS` *before* the generic `ark-research → /ark:research` pass, preventing double-rewrite. The closed-form rule in C-11 is therefore implementation-faithful.
- V-002 is the binding constraint on commit. Until it is resolved the deep-tier SPEC extraction would silently truncate the promoted SPEC; that has knock-on effects for future iterations and for SPEC-as-living-record discipline.
- Pre-existing V-IT-1 / V-IT-2 / V-IT-3 / V-E-1 task-label comments in `new_tests.rs:450/452/478/505/523` predate this task and are out of scope for V-001 — they are recorded here only to clarify that V-001's recommendation lists exactly the new sites, not the legacy ones.
